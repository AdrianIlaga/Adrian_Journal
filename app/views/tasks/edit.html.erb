<div class="w-full flex flex-col items-center">
    <%= render "partials/alert", page: @category%>
</div>

<div class="container grid grid-cols-12 gap-6 px-24 py-16">

    <div class="col-span-6">
        <%# Insert flash picture or words here %>
    </div>

    <div class="col-span-6 bg-yellow-50 shadow-lg">
        <%= form_with model: [@category, @task], local: true do |f| %>
            <%= render "form", f: f %>
        <% end %>
    </div>

</div>