<div>
    <h1><%= @category.title %> </h1>
    <p><%= @category.description %> </p>
</div>
<%= render "tasks/index", category: @category %>

<%= link_to 'New task', new_category_task_path(@category) %>
